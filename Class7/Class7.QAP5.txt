Create a disk file containing an array of JSON data. Read this file from disk and iterate through the whole array using the forEach() method for the array object. For each record in the JSON file display one or more of the fields of content to the browser console.